// Expo Router screen for `/collector-modal`. The file's location under
// `src/app/` is what wires it into the file-system router; pairs with
// the `<Stack.Screen name="collector-modal" presentation="modal" />`
// declaration in `_layout.tsx`. The path matches
// `<CollectorHostProvider modalPath>`'s default.
export { CollectorModalRoute as default } from 'collector-expo'
