{
  "name": "branding-core",
  "version": "0.0.0",
  "private": true,
  "description": "Pure branding constants for Wildflower Health: site origin, section paths (the deploy contract), navigation link data, anchor-href resolution, and the per-app introduction copy shared by the homepage and the apps' landing pages. No DOM, no platform imports.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "vp pack",
    "check": "vp check",
    "dev": "vp pack --watch",
    "test": "vp test"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "fast-check": "catalog:",
    "kitchen-sink": "workspace:*",
    "typescript": "catalog:",
    "vite-plus": "catalog:"
  }
}
