// Stub for the `tunnel-core/contexts` entry point.
// The real content (e.g. `OriginFromServedOrigin`) lands in a follow-up PR;
// this file exists so `vp pack` and the `./contexts` export resolve.
export type ContextsEntryPlaceholder = never
