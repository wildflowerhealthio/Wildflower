{
  "name": "positioned-text",
  "version": "0.0.0",
  "private": true,
  "description": "A PDF's text content as absolutely positioned runs: the `Document.Schema` Effect Schema (`wildflower-positioned-text` v1) and `Document.FromJson`. The neutral format seam of the file-formats slice, below both the importer (a dialect parses it into records) and the anonymizer (it masks and downloads it). No DOM, no fs, no React, no extraction library.",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Assessment-is/wildflower.git",
    "directory": "slices/file-formats/positioned-text"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "vp pack",
    "check": "vp check",
    "dev": "vp pack --watch",
    "test": "vp test"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "effect": "catalog:",
    "fast-check": "catalog:",
    "kitchen-sink": "workspace:*",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "peerDependencies": {
    "effect": "catalog:"
  }
}
