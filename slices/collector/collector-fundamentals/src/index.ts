// At present, nothing is exported from the root — consumers use the
// sub-entries directly (`collector-fundamentals/model`,
// `collector-fundamentals/test-helpers`). The package.json `exports`
// map is the canonical surface.
// oxlint-disable-next-line unicorn/require-module-specifiers
export {}
