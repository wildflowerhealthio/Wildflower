// Rexall Be Well collector. The full collector surface: the
// `RexallCollectorDescriptor` (config, scraping plan, persist sink, display)
// `collector-registry` assembles, and the `RexallConfigForm` `collector-react`
// registers. The carebook STU3 dialect, response kinds, and source descriptor
// live in `rexall-be-well-source`.

export * from './config.ts'
export * from './rexall-config-form.tsx'
