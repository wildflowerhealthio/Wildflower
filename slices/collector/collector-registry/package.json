{
  "name": "collector-registry",
  "version": "0.0.0",
  "private": true,
  "description": "Assembly/registry layer for the collector slice — the most-downstream TS package: it statically imports each concrete `*-client-collector` and assembles their configs into a closed, compile-time discriminated union, alongside the CollectorBridge wire contract, CollectorBridgeMessageHandler dispatcher, and the HTTP API definition for the remotes endpoints. Shared model primitives (and the `./config-form` view contract) live in `collector-fundamentals`; concrete clients (e.g. `fhir-r4-client-collector`) depend on `collector-fundamentals` only — including for their config-form UI — never on the `collector-react` adapter.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Assessment-is/wildflower.git",
    "directory": "slices/collector/collector-registry"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "default": "./dist/index.js"
    },
    "./registry": {
      "source": "./src/registry.ts",
      "default": "./dist/registry.js"
    },
    "./clients": {
      "source": "./src/clients/index.ts",
      "default": "./dist/clients/index.js"
    },
    "./http-api-definition": {
      "source": "./src/http-api-definition/index.ts",
      "default": "./dist/http-api-definition/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "vp pack",
    "check": "vp check",
    "dev": "vp pack --watch",
    "test": "vp test"
  },
  "devDependencies": {
    "@effect/platform": "catalog:",
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "browser-sniffer-core": "workspace:*",
    "collector-fundamentals": "workspace:*",
    "effect": "catalog:",
    "effect-messaging-core": "workspace:*",
    "fast-check": "catalog:",
    "fhir-r4-client-collector": "workspace:*",
    "kitchen-sink": "workspace:*",
    "rexall-be-well-collector": "workspace:*",
    "shared-structures-core": "workspace:*",
    "shoppers-drugmart-collector": "workspace:*",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "web-trace-collector": "workspace:*"
  },
  "peerDependencies": {
    "@effect/platform": "catalog:",
    "browser-sniffer-core": "workspace:*",
    "collector-fundamentals": "workspace:*",
    "effect": "catalog:",
    "effect-messaging-core": "workspace:*",
    "fhir-r4-client-collector": "workspace:*",
    "kitchen-sink": "workspace:*",
    "rexall-be-well-collector": "workspace:*",
    "shared-structures-core": "workspace:*",
    "shoppers-drugmart-collector": "workspace:*",
    "web-trace-collector": "workspace:*"
  }
}
