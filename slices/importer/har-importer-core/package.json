{
  "name": "har-importer-core",
  "version": "0.0.0",
  "private": true,
  "description": "The HAR binding of the importer slice: the concrete FileImporterDescriptor for format 'har' — HAR decode via web-trace-core's codec, the FHIR R4 response-kind pool, HAR settings, and the FHIR persist sink (withMetaSource + persistResources). No DOM, no fs, no React.",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Assessment-is/wildflower.git",
    "directory": "slices/importer/har-importer-core"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "vp pack",
    "check": "vp check",
    "dev": "vp pack --watch",
    "test": "vp test"
  },
  "devDependencies": {
    "@effect/platform": "catalog:",
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "effect": "catalog:",
    "fast-check": "catalog:",
    "fhir-r4": "workspace:*",
    "fhir-r4-source": "workspace:*",
    "http-extraction-fundamentals": "workspace:*",
    "importer-fundamentals": "workspace:*",
    "kitchen-sink": "workspace:*",
    "typescript": "catalog:",
    "vite-plus": "catalog:",
    "web-trace-core": "workspace:*"
  },
  "peerDependencies": {
    "effect": "catalog:",
    "fhir-r4": "workspace:*",
    "fhir-r4-source": "workspace:*",
    "http-extraction-fundamentals": "workspace:*",
    "importer-fundamentals": "workspace:*",
    "web-trace-core": "workspace:*"
  }
}
