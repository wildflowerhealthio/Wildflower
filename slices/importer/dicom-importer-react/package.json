{
  "name": "dicom-importer-react",
  "version": "0.0.0",
  "private": true,
  "description": "The DICOM format's UI for the importer slice: a no-op DicomSettingsPicker (no user-facing knobs yet) — the same empty settings the descriptor carries. Presentation and interaction only.",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Assessment-is/wildflower.git",
    "directory": "slices/importer/dicom-importer-react"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "vp pack",
    "check": "vp check",
    "dev": "vp pack --watch",
    "test": "vp test"
  },
  "devDependencies": {
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "dicom-importer-core": "workspace:*",
    "importer-fundamentals": "workspace:*",
    "react": "catalog:",
    "react-dom": "catalog:",
    "typescript": "catalog:",
    "vite-plus": "catalog:"
  },
  "peerDependencies": {
    "dicom-importer-core": "workspace:*",
    "importer-fundamentals": "workspace:*",
    "react": "catalog:"
  }
}
