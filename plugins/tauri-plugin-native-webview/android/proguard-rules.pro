# Add project-specific ProGuard rules here. None are required for the
# native-webview plugin today (minification is disabled in the release build
# type), but the file must exist because build.gradle.kts references it.
